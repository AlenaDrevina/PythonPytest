pipeline {
    agent any
    stages {
      stage('Build') {
        steps {
          git branch: "${params.BRANCH}", url: 'https://github.com/AlenaDrevina/PythonPytest'
          sh 'python3 t1.py'
        }
      }
      
    }    
  }

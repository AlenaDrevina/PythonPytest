pipeline {
    agent any
    stages {
   
      stage('Build') {
        steps {
          git branch: 'main', url: 'https://github.com/AlenaDrevina/PythonPytest.git'
        }
      }
      stage('Test') {
        steps {
          sh 'python3 t5.py'
        }
      }
      stage('Deploy') {
        steps {
         sh 'git stash'
         sh 'git checkout pre-prod'
         sh 'git merge main'
         sh 'git add .'
         sh 'git push pre-prod'
        }
      }
      stage('Monitor') {
     steps {
       echo 'Well done!'
     }
   }
    }    
  }
